import type { App } from "@slack/bolt";

// Create command logic has been moved to app.ts for direct modal handling.
// This file is kept for structural reference only.

export function registerCreateCommand(_app: App): void {
  // No-op: create command routing and modal submission are handled in app.ts
}
